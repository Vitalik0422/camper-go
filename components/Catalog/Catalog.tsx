import clsx from 'clsx';
import React from 'react';

const Catalog = () => {
  return (
    <section>
      <div className={clsx('container')}>container</div>
    </section>
  );
};

export default Catalog;
